import React from 'react';
import Nav from './Nav';
import './Header.css'

const Header = () => {
  return(
      <header className='nav-header'>
          <Nav />
      </header>
  );
};

export default Header;
